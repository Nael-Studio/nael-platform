---
title: 'Interceptors · Microservices'
description: 'Wrap message handlers with @UseInterceptors and the microservice execution context.'
---

import { Badge } from '@/components/ui/badge';
import { CodeBlock } from '@/components/shared/simple-code-block';

export const interceptorExample = `import { Injectable } from '@nl-framework/core';
import type {
  MicroserviceInterceptor,
  MicroserviceExecutionContext,
  CallHandler,
} from '@nl-framework/microservices';

@Injectable()
export class TimingInterceptor implements MicroserviceInterceptor {
  async intercept(context: MicroserviceExecutionContext, next: CallHandler) {
    const started = Date.now();
    try {
      return await next.handle();
    } finally {
      console.log('handler duration', context.getPattern(), Date.now() - started);
    }
  }
}`;

export const controllerSample = `import { Controller, UseInterceptors } from '@nl-framework/core';
import { MessagePattern } from '@nl-framework/microservices';
import { TimingInterceptor } from './interceptors/timing.interceptor';

@Controller()
@UseInterceptors(TimingInterceptor)
export class OrdersConsumer {
  @MessagePattern('orders.create')
  async create(order: OrderDto) {
    return { ok: true };
  }

  @MessagePattern('orders.cancel')
  @UseInterceptors(TimingInterceptor)
  async cancel(order: OrderDto) {
    return { ok: true };
  }
}`;

<article className="space-y-8">
  <div className="space-y-3">
    <Badge className="bg-amber-100 text-amber-900 dark:bg-amber-900/30 dark:text-amber-50">Microservices</Badge>
    <h1 className="text-4xl font-semibold tracking-tight">Interceptors</h1>
    <div className="text-lg text-muted-foreground max-w-3xl">
      Microservice controllers reuse the <code>@UseInterceptors()</code> decorator from <code>@nl-framework/core</code>.
      The dispatcher runs the interceptor chain around each handler, after guards and around the pipe/handler step.
    </div>
  </div>

  <section className="space-y-4" id="status">
    <h2 className="text-2xl font-semibold">How it runs</h2>
    <ul className="list-disc space-y-2 pl-5 text-muted-foreground">
      <li><code>UseInterceptors()</code> attaches metadata at both the class and handler level, identical to HTTP and GraphQL.</li>
      <li>Interceptors nest global → controller → method around the handler; each may run logic before and after <code>next.handle()</code>.</li>
      <li>Register cross-cutting interceptors globally with <code>registerMicroserviceInterceptor()</code>.</li>
    </ul>
  </section>

  <section className="space-y-4" id="create">
    <h2 className="text-2xl font-semibold">Author an interceptor</h2>
    <div className="text-muted-foreground">
      Implement <code>MicroserviceInterceptor</code> (or provide a plain function of <code>(context, next)</code>). The
      execution context exposes the pattern, payload, and metadata so logic can stay transport-agnostic.
    </div>
    <CodeBlock code={interceptorExample} title="interceptors/timing.interceptor.ts" />
  </section>

  <section className="space-y-4" id="apply">
    <h2 className="text-2xl font-semibold">Apply with @UseInterceptors()</h2>
    <div className="text-muted-foreground">
      Decorate the controller for broad coverage or a single handler for scoped interception, matching HTTP semantics.
    </div>
    <CodeBlock code={controllerSample} title="orders.consumer.ts" />
  </section>

  <section className="space-y-4" id="behavior">
    <h2 className="text-2xl font-semibold">Behavior</h2>
    <ul className="list-disc space-y-2 pl-5 text-muted-foreground">
      <li>Interceptors execute global → controller → method, wrapping the handler like around-advice.</li>
      <li>Return the value from <code>next.handle()</code>, transform it, or skip <code>next</code> entirely to short-circuit (e.g. a cache hit).</li>
      <li>Keep interceptors stateless or inject services so they stay transport-agnostic.</li>
    </ul>
  </section>
</article>

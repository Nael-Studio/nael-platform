import { documentationResourceProvider } from './documentation';
import type { ResourceProvider } from './types';

export const resourceProviders: ResourceProvider[] = [documentationResourceProvider];

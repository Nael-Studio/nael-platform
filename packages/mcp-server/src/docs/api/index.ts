import { classReference } from './classes';
import { decoratorReference } from './decorators';
import { interfaceReference } from './interfaces';

export const apiReference = {
  decorators: decoratorReference,
  classes: classReference,
  interfaces: interfaceReference,
};

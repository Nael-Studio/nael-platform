{
  "name": "@nl-framework/example-graphql-subscriptions",
  "private": true,
  "version": "0.1.0",
  "description": "GraphQL subscriptions example: a chat feed over graphql-ws on Bun.",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "test": "bun test",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@nl-framework/core": "workspace:*",
    "@nl-framework/graphql": "workspace:*",
    "@nl-framework/logger": "workspace:*"
  },
  "devDependencies": {
    "graphql-ws": "^5.16.0"
  }
}

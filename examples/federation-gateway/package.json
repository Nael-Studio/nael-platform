{
  "name": "@nl-framework/example-federation-gateway",
  "private": true,
  "version": "0.1.0",
  "description": "Apollo Federation gateway example combining nl-framework subgraphs into a supergraph.",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@apollo/gateway": "^2.7.4",
    "@apollo/server": "^5.0.0",
    "@nl-framework/core": "workspace:*",
    "@nl-framework/http": "workspace:*",
    "@nl-framework/logger": "workspace:*",
    "@nl-framework/platform": "workspace:*",
    "graphql": "^16.8.1"
  }
}
